set -e -x

export GOROOT=$(readlink -nf /var/vcap/packages/golang1.9.1)
export PATH=$GOROOT/bin:$PATH
export GOPATH=$PWD/loggregator

go build -o ${BOSH_INSTALL_TARGET}/metron code.cloudfoundry.org/loggregator/agent
