#!/bin/bash

$(dirname $0)/go test -i --race dea_logging_agent
$(dirname $0)/go test -v --race dea_logging_agent

$(dirname $0)/go test -i --race loggregator
$(dirname $0)/go test -v --race loggregator
