#!/bin/bash

set -e

SCRIPT_HOME=$( cd "$( dirname "$0" )" && pwd )

base=$SCRIPT_HOME/..:$SCRIPT_HOME/../dea_logging_agent:$SCRIPT_HOME/../traffic_controller:$SCRIPT_HOME/../server:$SCRIPT_HOME/../protobuf_tester

exec env GOPATH=$base $@

